services:
  backend:
    image: ner-uz-solution:a100-final
    build: .
    gpus: all
    ports:
      - "8000:8000"
    restart: unless-stopped
  frontend:
    build:
      context: ./apps/frontend
    ports:
      - "3000:8080"
    depends_on:
      backend:
        condition: service_healthy
