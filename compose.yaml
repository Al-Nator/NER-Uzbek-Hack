services:
  backend:
    extends:
      file: backend/compose.yaml
      service: backend
    build:
      context: ./backend
  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:8080"
    depends_on:
      backend:
        condition: service_healthy
